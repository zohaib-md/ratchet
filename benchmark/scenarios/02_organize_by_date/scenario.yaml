name: organize_by_date
description: Baseline task - organize files by modification date

task_description: |
  Organize the files in this directory by their modification date:
  - Create directories in YYYY-MM format (e.g., 2026-07, 2026-08)
  - Move each file into the directory matching its modification month

protected_files: []

success_checks:
  - type: directory_exists
    path: "2026-08"
